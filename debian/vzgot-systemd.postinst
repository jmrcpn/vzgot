#!/bin/sh
set -e

if [ "$1" = "configure" ]; then
    # Rechargement de NetworkManager si présent, sans passer par systemctl direct
    if command -v NetworkManager >/dev/null 2>&1; then
        invoke-rc.d NetworkManager reload 2>/dev/null || true
    fi
fi

#DEBHELPER#

exit 0
