#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    # Rechargement propre de NetworkManager sans appeler systemctl
    if command -v NetworkManager >/dev/null 2>&1; then
        invoke-rc.d NetworkManager reload 2>/dev/null || true
    fi
fi

#DEBHELPER#

exit 0
